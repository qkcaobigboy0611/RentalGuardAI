/**
 * @author qkcao
 * @date 2026/1/27 17:57
 */
package com.rental.guard.ai.domain.service.v1;

/**
 * 混合记忆管理器：结合多种存储策略
 * 是否使用向量数据库取决于具体的需求。向量数据库主要用于语义的搜索，即内容的相似性进行搜索。
 * （1）存储和检索用户的历史对话，以便在当前对话中参考
 * （2）存储已知的欺诈案例，用于与当前对话进行对比
 * （3）存储用户的行为模式，用于异常检测
 * 但是，对于会话管理本身，通常关注对话的上下文（即最近的对话历史），而不是从海量历史对话中检索相似的对话。
 * 因此，对于会话管理方式：
 * （1）短期记忆：使用缓存存储当前对话的上下文，包括最近的几轮对话，这样可以在处理请求时，快递获取上下文。
 * （2）长期记忆：如果需要长期保存会话的话，可以使用关系型数据库进行保存。但是这些数据通常用于后续的分析和训练，而不是实时检索。
 * （3）向量数据库：如果想要实现基于语义的案例检索，例如从历史的欺诈案例中找出与当前对话相似的案例，那么我们可以使用向量数据库。
 * 但是这通常是用于增强分析模块，而不是会话管理本身。
 *
 * 因此在防租房欺诈智能体中，向量数据库一般用于欺诈分析模块，用于检索相似案例。
 *
 */
public class HybridMemoryManager {

}
